import { Inter, Syne, Nunito } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const syne = Syne({ subsets: ['latin'], variable: '--font-syne' });
export const nunito = Nunito({ subsets: ['latin'] });